{
  "name": "mernproject",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd react-bank-system && npm run build",
    "install-client": "cd react-bank-system && npm install",
    "heroku-postbuild": "npm run install-client && npm run build",
    "start": "node server.js",
    "client:build": "cd react-bank-system && npm run build",
    "client": "cd react-bank-system && npm start"
  },
  "author": "Kushagra",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.11.7"
  }
}
